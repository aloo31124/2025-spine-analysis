2026-06-28 08:22:17

## git commit
update: #4 [客戶管理] 推薦枕頭彈窗上方顯示型號命名建議計算結果
- PillowRecommendationModal 新增 modelName prop，於彈窗 body 最上方顯示「型號命名建議」，有結果顯示型號、無則顯示「尚未計算」
- CreateEditCustomer 於 render 前以 composeModelName 計算 modelName 並傳入彈窗，計算來源與下方「型號命名建議」區塊一致
- 新增 modelNameResult / modelNameValue / modelNameEmpty 樣式

## prompt 1
客戶管理頁面，枕頭推薦視窗上方，請顯示 型號命名建議的計算結果，若無則顯示尚未計算。

## prompt 1 response
- 定位相關檔案：PillowRecommendationModal.jsx / CreateEditCustomer.jsx / spineRecommendation.js（composeModelName 計算型號命名）
- CreateEditCustomer.jsx：於 render 前以 composeModelName 計算 modelName（來源：defaultHeight、computeShimAdjustment(spinePoint37Distance, defaultHeight)、spinePillowRecommendation），與下方「型號命名建議」區塊邏輯一致，並以 prop 傳入彈窗
- PillowRecommendationModal.jsx：新增 modelName prop，於彈窗 body 最上方顯示「型號命名建議」，有結果顯示型號（如 8.5FAA）、無則顯示「尚未計算」
- PillowRecommendationModal.module.css：新增 modelNameResult / modelNameValue / modelNameEmpty 樣式（綠色結果區塊、灰字斜體尚未計算）
- modelName 僅四項數值（高度、墊片、弧度型號）齊全才組出型號，否則為 null，彈窗即顯示「尚未計算」
