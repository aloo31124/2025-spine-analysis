2026.02.28




[refactor] #100 [分析尾椎] [頸椎分析] 提升可讀性。
[why] 為了改善程式碼結構與可維護性，並整合重複邏輯。
[how]
- 移除不必要的狀態與效應，使用自定義 hook `useAnalysisBase` 來管理分析邏輯。
- 簡化點位初始化與計算邏輯，將相關功能集中在 `buildTailMetrics` 與 `computeResults` 函數中。
- 更新 UI 元件的渲染邏輯，減少重複程式碼，提升可讀性。
- 抽取至 useAnalysisBase 的共用邏輯
    * 照片載入（localStorage / location.state）
    * 點位拖曳操作（mouseDown / mouseMove / mouseUp + 全域事件監聽）
    * 縮放控制（applyScale / zoomIn / zoomOut）
    * 儲存/客戶綁定流程（bindCustomer / createNewCustomer / selectCustomer / saveAnalysisResult）
    * 空白畫面切換、容器背景樣式、比例尺狀態管理
    * calculateDistance 工具函式



[refactor] 頸椎分析 拔除裁切功能、編輯視圖、結果視圖、攝影點位拖曳模態框，以及相關狀態、handlers 和 JSX，保留核心流程。
Removed:
- 裁切(Crop)功能 — cropArea/isDragging/dragStart 狀態、editorImageRef/cropAreaRef refs、handleCropPhoto/handleCropMouseDown/
- handleCropMouseMove/handleCropMouseUp handlers、crop 拖拽 useEffect 監聽
- 編輯(Editor)視圖 — renderEditorView 及其底部按鈕（裁切/取消）
- 結果(Result)視圖 — renderResultView 及其底部按鈕（與 preview 重複）
- 攝影點位拖曳模態框 — showDragModeModal 狀態、
    handleOpenDragMode/handleSelectDragMode/handleCloseDragModeModal handlers 及對應 JSX（原已在 UI 中被註解隱藏）
- 未使用的 imports — FaCrop、FaSave、FaTimes
- 保留的核心流程： main（拍照/上傳） → preview（預覽 + 開始分析/重新拍攝） → 分析選擇模態框 → 導航至分析頁面，以及攝像頭即時拍攝 overlay。







