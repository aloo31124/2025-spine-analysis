2025.10.30




## git commit
[feat] [頸椎分析]綁定 客戶 資料 , 存入 db: CustomerAnalysisResult   a8275ca

[feat] [頸椎分析] 頸椎分析 [儲存結果] 後 [綁定客戶] 顯示於 客戶編輯頁。  8f47f66


[text](../../spine-server/src/models/customer.model.js)

[text](../../spine-client/src/pages.spine/manager/AnalysisSpine.jsx)

[text](../../spine-client/src/pages.spine/manager/CustomerAddPage.jsx)
[text](../../spine-client/src/pages.spine/manager/CustomerEditPage.jsx)

[text](../../spine-client/src/components/manager/CreateEdit/CreateEditCustomer.jsx)

## github copilot

### agent1:計算結果綁定到客戶

AnalysisSpine.jsx 計算分析結果後, 會產生 [儲存結過]按鈕, 並隱藏 [計算]按鈕,
使用者按下 [儲存結果]按鈕後, 會跳出視窗有兩個按鈕:
[綁定客戶] 與 [新建客戶], 不論選擇哪個都會將其存入 客戶分析結果表(CustomerAnalysisResult) 中,
若 [綁定客戶] ,
- 則會在原畫面跳出客戶選擇視窗,
- 選擇後將該客戶userId存入 客戶分析結果表(CustomerAnalysisResult) 中,
- 最後 跳轉到 客戶編輯頁面 CustomerEditPage.jsx,
若 [新建客戶] , 
- 則直接開啟新建客戶頁數 CustomerAddPage.jsx, 
- 新建後將該新建客戶userId存入 客戶分析結果表(CustomerAnalysisResult) 中,

參考 customer.model.js 新增 createCustomerAnalysisResult.model.js ,
並在 AnalysisSpine.jsx 中呼叫 API 儲存結果,
該 API 新增 customerAnalysisResult.controller.js 與 customerAnalysisResult.service.js 另外處理,
並在 customer.model.js 中新增 CustomerAnalysisResult 資料表結構。


### 手動1:調整 客戶資訊

spine-client\src\pages.spine\manager\AnalysisSpine.jsx
```javascript

    // 處理綁定客戶
    const handleBindCustomer = async () => {
        try {
            // 取得客戶列表
            const searchParam = {};
            const pagingParam = { pageIndex: 1, pageSize: 1000 };
            const response = await getCustomerList(searchParam, pagingParam);
            if (response.status === 200) {
                setCustomerList(response.data.result.customerList || []);
                setShowSaveOptions(false);
                setShowCustomerModal(true);
            }
        } catch (error) {
            console.error('取得客戶列表錯誤:', error);
            alert('取得客戶列表失敗');
        }
    };

```



### agent2:客戶頁面顯示分析多筆分析結果
商品新增,編輯頁面分別為 CustomerAddPage.jsx 與 CustomerEditPage.jsx,
共用原件為 CreateEditCustomer.jsx, 
若 [綁定客戶] 將該筆分析結果存入 資料庫 CustomerAnalysisResult db表內,
依照 userId 查詢 CustomerAnalysisResult表 內該客戶所有分析紀錄,
並顯示於 共用元件CreateEditCustomer.jsx [分析結果]區域內。

若 [新建客戶] 因 user表還是空的, userId 尚未產生, 
 db CustomerAnalysisResult表內 userId 為 temp值,
但 分析結果仍需要顯示於 共用元件CreateEditCustomer.jsx [分析結果]區域內。
該新建客戶儲存後, 將 userId 更新至資料庫 CustomerAnalysisResult表內。

### agent2:客戶頁面顯示分析多筆分析結果.失敗
- 分析結果畫面無法顯示

spine-client\src\components\manager\CreateEdit\CreateEditCustomer.jsx

### agent3:
選擇 [綁定客戶] 後, 跳出客戶選擇視窗,
先將該筆分析資料與 userId 存入 firestore db CustomerAnalysisResult表內,
選擇客戶後, 跳轉至 客戶編輯頁面 CustomerEditPage.jsx,
其內部之共用元件為 CreateEditCustomer.jsx 用於顯示 [分析結果]。
[分析結果] 從 CustomerAnalysisResult表內取得, 並顯示於 CreateEditCustomer.jsx 的 [分析結果] 區域內。









