2026-06-27 10:50:04

## git commit
fix: #4 [登入驗證] 修正登出後「請重新登入」重複跳出多次

- 原因：App.js 路由巢狀使用 AppRouterVerify（外層 layout + 內層 page），登出清除 jwt 後兩個驗證器各自驗證失敗、各跳一次 alert，導致訊息重複顯示。
- 於 `AppRouterVerify.jsx` 新增模組層級旗標 `isReloginPrompted` 與 `promptRelogin()`，並發/巢狀驗證器只提示一次「請重新登入」，其餘僅導回 `/auth/login`。
- 以 `setTimeout` 於下一輪事件循環重置旗標，確保日後 token 過期仍能再次提示。
- 將 `if(!payload)` 與 `catch` 兩處重複 alert 統一收斂至 `promptRelogin()`。

## prompt 1
登出後會重複顯示多次顯示 請重新登入，修正只顯示一次就夠了，找出原因並修正。

## prompt 1 response
- 以 Grep 定位 `請重新登入` 來源，鎖定 `AppRouterVerify.jsx`（第 59、66 行兩處 alert）。
- 追查 `App.js` 路由結構，發現根因：`AppRouterVerify` 被巢狀使用——例如 `/manager/analysis/spine` 會同時掛載外層 `ManagerLayout`(node=seller) 與內層 `AnalysisSpine`(node=analysis-spine) 兩個驗證器。
- 登出後 `localStorage` 的 `jwt` 被移除，`verifyJwt()` 在兩個驗證器都回傳 null/失敗，於是各自跳一次 `alert("請重新登入")`，造成重複多次。
- 修正：在 `AppRouterVerify.jsx` 加入模組層級共用旗標 `isReloginPrompted` 與 `promptRelogin()` 去重函式；第一個失敗者才 alert，其餘僅 `navigate('/auth/login')`。
- 以 `setTimeout`（macrotask，於所有並發 microtask 之後執行，不受使用者按掉 alert 的時間影響）於下一輪事件循環重置旗標，確保日後 token 過期仍能再次提示。
- 將 `if(!payload)` 與 `catch` 兩處重複 alert 收斂到單一 `promptRelogin()`；不論巢狀幾層，登出後最多只跳一次。
- 產出對應 git commit 區塊。

## prompt 2
（使用者觸發 /log-prompt，要求把本次對話歸檔為 prompt 紀錄）

## prompt 2 response
- Glob 查 `log/prompt20260627-*.md`，當日已有流水號 1，本份取 2。
- 以 PowerShell 取得時間戳 `2026-06-27 10:50:04`。
- 參照既有範本格式，寫出 `.claude/skills/log-prompt/log/prompt20260627-2_修正登出後請重新登入重複跳窗.md`，含時間戳、git commit、prompt 逐字與回復精要。
