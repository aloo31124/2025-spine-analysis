

===============================================================================

如下 res1, res2, res3 使用 axios 處理異步請求,
res1, res2 不需互相等待, 但 res3 需等待 res2 該如何整理讓其更精簡？

newshop-client/src/components/authRegisterInfoPage.jsx
    function AuthRegisterInfoPage() {
        const clickSendRegistInfo = async () => {
            const res1 = await postMail(mail);
            const res2 = await postUser(mail);
            const newUser = res2.data.newUser;
            console.log("newUser : ", newUser);
            const res3 = await updateUser({...newUser, account, phone});
            console.log('res3 : ', res3);
        }

        return (... 畫面)

newshop-client/src/api/auth.js
import axios from 'axios';
    const BASE_URL = 'http://localhost:8081/api/auth';
    export const postMail = async (mail) => {
        return axios.post(`${BASE_URL}/regist/mail`, {mail});
    }
    export const postUser = async (mail) => {
        return axios.post(`${BASE_URL}/regist/user/add`, {mail})
    }
    export const updateUser = async (newUser) => {
        return axios.post(`${BASE_URL}/regist/user/info`, {newUser});
    }


===============================================================================


===============================================================================




